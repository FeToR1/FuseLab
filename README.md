
# Docker

``` bash
docker-compose up -d 
docker exec -it fuse_ubuntu bash
```

# Ubuntu

## Python

``` bash
python3 /py_source/main.py /filesystem
cd /filesystem
```

## CPP

``` bash
make -C /source
./source/main /filesystem
cd /filesystem
```
